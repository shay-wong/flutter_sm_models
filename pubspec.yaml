name: flutter_sm_models
description: "Some general models for personal use."
version: 0.0.2
homepage: "https://github.com/shay-wong/flutter_sm_models"
repository: "https://github.com/shay-wong/flutter_sm_models"

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ">=4.6.6 <5.0.0"
  json_annotation: ">=4.8.1 <5.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  json_serializable: ">=6.7.0 <7.0.0"
  build_runner: ^2.4.8

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
